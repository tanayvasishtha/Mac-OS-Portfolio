# Mac-OS-Portfolio

A macOS-style portfolio website that mimics the M4 MacBook interface — dock, windows, Terminal, System Settings (wallpaper picker), and more. Built with React, Vite, Tailwind CSS, and GSAP.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
